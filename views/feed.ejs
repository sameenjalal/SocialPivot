<%- partial('header.ejs', {session: session}) %>

<h1>What's New</h1>

<% for(var i=0; i<activity.length; i++) { %>
	<div class="well">
		<div class="row">
		<% var a = activity[i];
		   if(a.idea) { %>
		   	 <div class="span6">
		   		 <h2><a href="/idea/<%= a._id %>"><%= a.name %></a></h2>
		   	 <div class="span3">
		   	 	 <span>Created by: <%= a.owner %></span>
		   	 </div>
		   	 <% if(a.prev !== null) { %>
		   	 <div class="span3">
		</div>
		<div class="row">
		   	 <ul>
		   	 	<% for(var j=0; j<a.tags.length; j++) { %>
		   	 		<li><a href="/search?search_terms=<%= a.tags[j] %>"><%= a.tags[j] %></a></li>
		   	 	<% } %>
		   	 </ul>
		</div>
		<div class
